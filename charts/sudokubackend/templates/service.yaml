apiVersion: v1
kind: Service
metadata:
  name: {{ template "sudokubackend.fullname" . }}
  labels:
    app: {{ template "sudokubackend.name" . }}
    chart: {{ template "sudokubackend.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "sudokubackend.name" . }}
    release: {{ .Release.Name }}
